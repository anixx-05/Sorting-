#include<stdio.h>
void inst_sort (intlI);
void main (
int num 5], count;
printf ("\n enter the five elements to sort:\n");
for (count = 0; count < 5; count++)
scanf (*%d", &num[count]):
inst_sort (num);
printf ("In\n elements after sorting:\n");
for (count = 0; count < 5; count++)
printf (*%d\n", num[count]):
void inst_sort (int num[l)
int 1, j, k;
for (= 1; j <5; j++)
k = num[il:
for (i =j - 1; i >= 0 && k < num[i]: i--)
num[i + 1] = num[i]:
num(i + 1] = k;
