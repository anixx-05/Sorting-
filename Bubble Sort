#include<stdio.h>
#include<conio.h>
void main O
int n, 1, J, temp. a(100];
printf ("Enter the total integers you want to enter (make it less than 100): \n"); scanf ("%d", &n):
printf ("Enter the %d integer array elements:)n", n); for (i =0; i <n; i++)
scanf ("%d", &a[iD:
for (i = 0; i <n - 1; i++)
for (j =0; j<n-i - 1; j++)
{
if (alj + 1] < alil)
temp = a[jl:
afl = al) + 1]:
alj + 1] = temp:
}
printf ("The sorted numbers are:");
for (i = 0; i < n; i++)
printf (*% 3d", afil):
getch (:
